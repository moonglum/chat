version: "3"
services:
  redis:
    image: "redis:6.0"
    restart: always
  chat:
    build: .
    command: npm start
    volumes:
      - .:/myapp
      - /myapp/node_modules
    ports:
      - "8000:8000"
    depends_on:
      - redis
